Installation instructions:

1. Clone the repo
2. cd into root directory
3. run the command:  ./install.sh
4. source the gym gazebo sim's ros2 workspace: source ros2_ws/install/setup.bash
4. import gym_gazebo into your project!

